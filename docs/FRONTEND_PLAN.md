# Cockpit v2 — Plan Frontend (React + TypeScript + Tailwind)

> Document vivant : la section "État actuel" ci-dessous est la version en vigueur. Tout changement de cap s'ajoute au **Journal** en bas du fichier, jamais en réécrivant l'historique.

## État actuel

### Contexte de décision

Issu d'une session de cadrage (2026-08-26) : le choix exact entre React/Vue a été délégué ("peu importe, là où tu as le plus de références"), avec une exigence ferme : **Tailwind CSS**. SSE retenu pour le temps réel (fin du polling 5s du legacy). Voir `LEGACY_FEATURES.md` §10 pour l'inventaire complet des pages/comportements à reproduire.

### Stack

- **Monorepo pnpm** : `apps/web` (Vite + React + TypeScript + Tailwind), partage de types/schémas Zod avec `apps/api` via `packages/shared`.
- **UI kit** : Tailwind + shadcn/ui (composants Radix headless stylés Tailwind).
- **Routing** : React Router.
- **Data fetching/cache** : TanStack Query.
- **Formulaires** : React Hook Form + Zod (schémas partagés avec les DTOs NestJS via `packages/shared`).
- **Temps réel** : hook `useTripEvents()` sur un flux SSE (`/api/events/stream`), invalide le cache TanStack Query correspondant à la réception d'un événement.

### Pages (mappées depuis l'inventaire legacy)

Authentifiées :
- `/login` — email+mot de passe puis code 2FA (un compte par utilisateur, plus un seul compte admin partagé comme au legacy).
- `/bookings` (ex-`dispatcher.html`) — barre de création de course + listes Local/Farm out.
- `/clients`, `/drivers`, `/vehicles` — CRUD + popups d'édition/indisponibilité, mêmes règles conditionnelles que le legacy.
- `/planning` — toggle chauffeurs/véhicules, vue liste + vue timeline Gantt drag&drop.
- `/events` — sélection/création de compte événement + "Create bulk".
- `/invoicing` — onglets Customer/Driver log/Partner log/History.
- `/finance` — stub (fidèle au legacy, qui n'a jamais eu de logique ici non plus).
- `/settings` (ex-`owner.html`) — infos société + gestion des utilisateurs (remplace le panneau "Access" décoratif du legacy par une vraie gestion de comptes).

Publiques (sans auth, comme le legacy) :
- `/driver/:ref` (ex-`chauffeur.html`) — page mobile chauffeur, 5 boutons d'étape.
- `/track/:ref` (ex-`dashboard.html`) — suivi client en lecture seule, **SSE au lieu de polling 5s**.

### Composants partagés à recréer (équivalents `common.js`)

- Combobox recherche (pays, référence client/chauffeur, POC, adresse via géocodage).
- `RecordDialog` — popup générique d'édition/création (remplace `openRecordModal`).
- Popup de confirmation générique.
- Timeline Gantt réutilisable (planning chauffeurs/véhicules), drag&drop d'assignation.
- Hint devise/FX (taux de change live).
- Popup infos vol (vérification FlightStats).
- Popups indisponibilité chauffeur/véhicule.

### Branding

Réutilisation des assets existants (`cockpit-icon.webp`, `cockpit-wordmark.png`), palette verte "WhatsApp" du legacy comme point de départ (`--green:#128C7E`), adaptée en tokens Tailwind (`theme.extend.colors`).

---

## Journal

> **2026-08-26 — Version initiale.** Issue de la session de cadrage (grilling) : React + TypeScript + Tailwind (choix du framework délégué, Tailwind imposé), monorepo pnpm partagé avec le backend, SSE pour remplacer le polling 5s legacy, pages mappées 1:1 depuis l'inventaire `LEGACY_FEATURES.md`.

> **2026-08-26 — Lancement de l'implémentation (grilling #2).** Le back (`apps/api`) est déjà largement implémenté (Auth/Users/Company/Meta/Clients/Drivers/Fleet/Notifications/Geo/Trips/Invoices/Realtime, tous testés e2e) ; `apps/web` était encore un scaffold Vite par défaut. Décisions de cadrage avant de coder :
> - **Ordre** : verticale complète d'abord (`/login` + `/bookings`, page la plus centrale du legacy `dispatcher.html`) plutôt qu'un scaffold en largeur de toutes les pages. Les autres pages authentifiées/publiques suivront en itérations séparées.
> - **Partage de types front/back** : le back utilise `class-validator` (pas Zod) et n'exposait aucun OpenAPI. Ajout de `@nestjs/swagger` sur les DTOs existants (non-invasif) + génération d'un client typé dans `packages/shared`, plutôt que des types dupliqués à la main ou rien de partagé — élimine le risque de dérive.
> - **Ajouts back additifs** décidés pour débloquer le front : `GET /api/auth/me` (identité + rôle courant, absent jusqu'ici, nécessaire pour le nav rôle-gated) et un provider WhatsApp "dev" (log console) sélectionné automatiquement quand `TWILIO_ACCOUNT_SID`/`TWILIO_AUTH_TOKEN` sont absents — sans ça, Dispatch/Notify/Advance-step (qui appellent `TwilioWhatsAppProvider`, codé en dur, sans fallback) seraient impossibles à tester en local.
> - **Données de test** : `prisma/seed-data.ts` étendu avec des clients/chauffeurs/véhicules flotte d'exemple (le seed actuel ne créait que l'admin + pays + types de véhicule ; le formulaire de course n'autorise aucune création à la volée).
> - **Fidélité legacy** : comportement/règles métier reproduits fidèlement (voir `LEGACY_FEATURES.md`), UI entièrement redessinée en shadcn/ui (pas de copie pixel du CSS artisanal legacy), accent vert de marque (`#128C7E`) conservé.
> - **Périmètre de la verticale bookings** : tout inclus dès cette passe — barre de création, tableaux Local/Farm out + filtres, popups d'édition rapide, annulation avec mot de passe manager (vérifie le mot de passe de l'utilisateur courant via `verify-password`, plus un secret partagé unique comme au legacy), dispatch-driver + advance-step (workflow de statut complet), upload nameboard, popup vérif vol (FlightStats, dégradé proprement si non configuré) et hint FX/marge live.
> - **Tests** : suite complète Vitest + Testing Library sur la logique à risque (règles de validation conditionnelles, machine à états du workflow de statut) + Playwright e2e sur le parcours création→dispatch→statuts, au même niveau de rigueur que les tests e2e déjà en place côté back.
> - **Dev workflow** : stack `docker compose up` (Postgres+api+web) utilisée pendant toute l'implémentation, vérification systématique au navigateur (pas seulement via les tests) avant de considérer un écran terminé.

> **2026-08-27 — Verticale Clients close, décision d'architecture pour les listes.** `/clients` terminé au même niveau de rigueur que Bookings (voir `docs/handoff/2026-08-27-frontend-clients-2.md`). Décision de cadrage explicite de l'utilisateur pendant cette session, applicable à toutes les verticales restantes (Drivers, Vehicles) : **ne pas répliquer les mauvais patterns techniques du legacy sous prétexte de fidélité** — la fidélité au legacy s'applique à la logique métier (règles de validation, calculs), pas à ses choix d'implémentation. Flaggé concrètement : `ClientsService.list()`/`DriversService.list()` (`findMany()` sans pagination ni filtre serveur, tout le filtrage côté front) reproduit tel quel le pattern legacy (`clients.html`/`drivers.html`) — accepté pour l'instant afin de ne pas bloquer la clôture de Clients, mais **à refondre en pagination/filtre serveur** avant de considérer l'architecture de liste comme définitive. Vérifier ce même piège avant de démarrer Drivers/Vehicles.

> **2026-08-27 — Pagination serveur + email validation/dedup, même session.** Revirement immédiat de la décision "on diffère" ci-dessus — traité dans la foulée plutôt que reporté (voir `docs/handoff/2026-08-27-frontend-clients-3.md`). `GET /api/clients` prend maintenant `search`/`type`/`includeInactive`/`page`/`limit` et renvoie une enveloppe paginée ; `page`/`limit` restent opt-in pour ne pas casser le combobox client de Bookings (qui a légitimement besoin du roster complet, comme le combobox pays). Email : format + déduplication (insensible à la casse) sur le champ `email`, format seul sur `pocEmail`, une seule implémentation (`packages/shared/src/validation/email.js`, réutilisée telle quelle par le schema Zod front et `ClientsService` back — pas de logique dupliquée). `DriversService.list()`/`FleetVehiclesService` gardent le même problème non résolu, à traiter à leur tour.

> **2026-08-27 — "Plus rien sans pagination" : le compromis opt-in ci-dessus est rejeté.** L'utilisateur refuse le mode "si `page`/`limit` sont omis, tout renvoyer" posé juste au-dessus pour le picker Bookings (voir `docs/handoff/2026-08-27-frontend-clients-4.md`) : **aucun endpoint de liste ne doit fonctionner sans borne**, quel que soit le cas d'usage — le bon pattern dépend du cas, pas une exemption. Pickers/dropdowns (Customer/Driver/Partner/Reg Nbr, filtres Bookings) passent en **request-on-demand** (recherche distante bornée, `useOptionMemory`+`SearchCombobox`) plutôt qu'en pagination classique — un combobox n'a pas besoin d'un tableau paginé. Le tableau Bookings (live, refresh SSE) passe à une **fenêtre de dates résolue côté serveur** (`period=upcoming/today/week/past/all`), reproduisant la règle `baseVisibility` qui était dupliquée côté front. `Drivers`/`FleetVehicles` reçoivent au passage le même traitement pagination/filtre que Clients (jusque-là aucun des deux n'avait ni l'un ni l'autre).

> **2026-08-27 — Verticale Drivers close.** `/drivers` construit au même niveau de rigueur que Clients (voir `docs/handoff/2026-08-27-frontend-drivers.md`) : deux tableaux Chauffeurs/Partenaires depuis la réponse paginée du back, validation conditionnelle miroir de `assertValidDriverFields()`, sélecteur d'Événement request-on-demand pour les chauffeurs `eventsOnly`, popup d'indisponibilité type-verrouillé, nouvelle permission `driver:reactivate` (suggérée par `docs/agents/permissions.md`, jusque-là non câblée). `ListPagination` extrait en composant partagé (`apps/web/src/components/list-pagination.tsx`) — Clients et Drivers l'utilisent tous les deux, plus de duplication. Reporté à la verticale `/vehicles` : le popup legacy de liaison véhicule↔partenaire (action côté Fleet, n'a de sens qu'une fois cette page construite).

> **2026-08-27 — Verticale Vehicles (Fleet) close.** `/vehicles` construit au même niveau de rigueur que Clients/Drivers (voir `docs/handoff/2026-08-27-frontend-vehicles.md`) : deux tableaux Internal/External **à colonnes différentes** (contrairement à Drivers), Nb Pax jamais éditable (recalculé côté client via `defaultFleetPax()`, porté du legacy), nouvelle permission `vehicle:reactivate`, et le popup de liaison véhicule↔partenaire différé par la session Drivers (câblé depuis `DriverCreateDialog` via une case "Ind." ou une confirmation Oui/Non pour les chauffeurs `eventsOnly`+Company). Bug notable trouvé et corrigé : un `<Select>` Radix en cascade (Catégorie→Marque→Modèle) n'affiche/n'enregistre une valeur que pour un `<SelectItem>` déjà rendu par ce composant — `form.setValue()`/`form.reset()` seuls ne suffisent pas si la liste d'options vient de changer dans le même rendu ; fix via une prop `key` dérivée des champs amont pour forcer un remontage propre à chaque transition. À retenir pour tout futur `<Select>` en cascade. Reporté, hors périmètre : le padlock "délier ce véhicule" côté Drivers (nécessiterait une nouvelle relation inverse `DriverEntity.fleetVehicle`).

> **2026-08-27 — Verticale Planning close.** `/planning` (voir `docs/handoff/2026-08-27-frontend-planning.md`) : une seule page, toggle Chauffeurs/Véhicules, vue Liste + vue Timeline (Gantt drag&drop codé à la main, pas de lib tierce — le legacy `renderTimeline` est un cas simple, positionnement en %, drag&drop HTML5 natif). Nouveau `PATCH /api/trips/:ref/assign` (patch léger driverRef/fleetRegNbr, indépendant du gros `update()`) plutôt que reconstruire un payload PUT complet côté front comme le faisait `quickUpdateTrip` au legacy. `GET /api/trips` gagne `category=daily|event|all` (défaut `daily`, comportement Bookings inchangé) pour le toggle Daily/Event/All. Ajout inédit sans équivalent legacy : indicateur "maintenant" sur le Gantt (ligne rouge + point), demandé lors d'une passe design après validation fonctionnelle — l'occasion aussi de sortir la colonne des libellés en `sticky` et de réintégrer la légende de couleurs par Catégorie que le premier portage avait omise. Piège d'outillage noté pour la suite : `locator.dragTo()` de Playwright s'est révélé flaky sur ce drag&drop HTML5 natif — remplacé par le pattern documenté par Playwright (dispatch manuel `dragstart`/`dragover`/`drop` avec un `DataTransfer` JSHandle partagé), déterministe.

> **2026-08-27 — Dette padlock "délier véhicule↔chauffeur" close.** Voir `docs/handoff/2026-08-27-frontend-drivers-vehicles-unlink.md`. Choix explicite de l'utilisateur entre les trois pistes proposées en fin de session Planning (`/events`, `/invoicing`, cette dette) — la plus petite des trois. La mutation (`PATCH /api/fleet-vehicles/:ref/driver`) et la relation Prisma (`Driver.fleetReserved`) existaient déjà ; contrairement à ce que notaient les handoffs Drivers/Vehicles précédents, il ne manquait qu'un maillon d'affichage — `DriverEntity.fleetReserved` (nouveau, via un `FleetVehicleBaseEntity` extrait pour éviter un cycle d'import avec `driver.entity.ts`) — pour que le cadenas puisse être rendu côté front (`drivers-table.tsx`, ligne `regNbr` + bouton sous le Nom du partenaire, `AlertDialog` de confirmation dans un nouveau `unlink-vehicle-dialog.tsx`). Aucune nouvelle permission (le padlock legacy est ungated, tout utilisateur connecté). Piège d'outillage noté : un run Playwright a échoué sur un import Vite fantôme (`@fontsource-variable/inter`, absent du code source) — cache `.vite` obsolète, `rm -rf apps/web/node_modules/.vite` a suffi.

> **2026-08-27 — Verticale Events close.** `/events` (voir `docs/handoff/2026-08-27-frontend-events.md`) : essentiellement une composition front — le modèle de données (`Client.clientType = EVENT` + 4 champs event), `GET /api/clients?type=event`, `GET /api/trips?category=event` et `POST /api/trips` existaient déjà (bâtis pour Drivers/Vehicles/Planning). Panneau "Select event" (picker request-on-demand + verrouillage du champ Customer de la barre de création, réutilisation de `TripFormFields` via deux nouvelles props ciblées `clientFieldDisabled`/`clientSeedOption` plutôt qu'une réécriture), flux "New" en **un seul dialog** réutilisant `ClientFormFields` (verrouillé sur Events via une nouvelle prop `typeLocked`) au lieu du chaînage à deux popups du legacy — écart délibéré, fidélité maintenue sur la logique métier (champs/validations identiques), pas sur l'UX du legacy. "Create bulk" (remplace "Create & Dispatch" — répartir un seul chauffeur n'a pas de sens sur un lot) : boucle client séquentielle (pas de nouvel endpoint bulk, le compteur de ref partagé imposant déjà le séquentiel côté legacy) portée dans `bulk-create.ts` (pur, testé unitairement) — règle de chaînage identique au legacy (jour 1 = PU/DO tapés, jours suivants = PU=DO=dropoff du jour 1, dernier jour forcé en ASD 4h par défaut si pas de dropoff). Nouvelle permission `client:create-past-event` (`ClientsService.create`, gate conditionnel façon `trip:edit-past`) pour la création d'un compte Events à date de début passée — seul ajout backend de la session. Bug latent découvert et corrigé (règle du projet : tout bug trouvé est corrigé, pas différé) : `eventStartDate`/`eventEndDate` sont des colonnes Prisma `DateTime` sérialisées en ISO complet ; injectées telles quelles dans un `<input type="date">` (qui n'accepte que `YYYY-MM-DD`), le champ apparaît vide. Touchait `event-select-panel.tsx`, `bulk-dates-dialog.tsx` (nouveaux cette session) et `client-form-mapping.ts`'s `clientToFormValues()` (existant, dialog d'édition `/clients`) — les trois corrigés via `.slice(0, 10)`, avec un test de régression (`client-form-mapping.test.ts`).

> **2026-08-27 — Verticale Invoicing close, dernière page authentifiée du plan.** `/invoicing` (voir `docs/handoff/2026-08-27-frontend-invoicing.md`) : le back (`apps/api/src/invoices/`) existait déjà en entier (endpoints, TVA configurable, compteur de ref persistant, transaction race-safe), donc session purement front. Onglets Customer (recherche + facturation, table dédiée `pending-trips-table.tsx` fidèle à `renderPendingTable` du legacy — pas une réutilisation de `BookingsTable`) et Partner log (même mécanique côté `trip.partner`, réutilise `BookingsTable`+dialogs) pleinement construits ; Driver log/History restent des placeholders "Coming soon" comme au legacy. Nouvelles dépendances `jspdf`/`jspdf-autotable`/`xlsx` pour la génération PDF/Excel 100% client-side (fidèle au legacy, qui le faisait déjà via CDN) — importées dynamiquement (pas en top-level) après que `pnpm build` a signalé un chunk principal >1.5MB à cause d'elles, ramené à ~880KB. Bug trouvé et corrigé immédiatement : `SearchCombobox`'s `role="combobox"` calcule son nom accessible depuis un label associé, pas depuis son contenu visuel — les comboboxes de barre de filtre (sans `<FormLabel>`) n'avaient donc aucun nom accessible ; nouvelle prop `aria-label` ajoutée au composant partagé. **Décision de cadrage notable** : questionné sur si Driver log/History méritent un "event log"/Event Sourcing, la réponse documentée dans le nouveau `docs/agents/event-log-design.md` est non — les deux besoins sont satisfaisables par une pagination/des filtres sur les tables `Invoice`/`Trip` existantes, sans nouvelle infrastructure. À lire avant de sortir ces deux onglets du statut placeholder.

> **2026-08-27 — Verticale Settings close** (pas d'entrée journal à l'époque, notée ici après coup) : `/settings` (voir `docs/handoff/2026-08-27-frontend-settings.md`) — session purement front, back déjà complet. Company info verrouillée définitivement après premier `PUT` (aucun endpoint de déverrouillage côté v2, contrairement au legacy) — gap accepté, documenté, pas contourné. Désactivation d'un user irréversible (pas d'endpoint de réactivation, contrairement à drivers/vehicles) — confirmation explicite via `AlertDialog` plutôt qu'un toggle inline.

> **2026-08-27 — Pages publiques `/driver/:ref` et `/track/:ref` closes, vraie fuite de données publique trouvée et corrigée au passage.** Voir `docs/handoff/2026-08-27-frontend-public-tracking.md`. Choix laissé à l'agent entre ces deux pages et le stub `/finance` — tranché en faveur de celles-ci après qu'un état des lieux a révélé que `GET /api/trips/:ref` et `POST /api/trips/:ref/notify` (déjà `@Public()` depuis la verticale Trips, jamais consommés côté front avant cette session) renvoyaient la `TripEntity` complète sans projection — prix, VAT, email/téléphone chauffeur+partenaire accessibles à quiconque tient un lien `ref`, sans authentification, contrairement au legacy dont le back pré-formatait une réponse minimale pour ces deux pages précises. Corrigé via une nouvelle `PublicTripEntity`/`toPublicTrip()` (mapper pur, `apps/api/src/trips/public-trip.mapper.ts`) : POC/instructions visibles seulement en vue chauffeur, jamais de prix/`client`/`driver` bruts dans aucune des deux vues. `GET /api/events/stream` (SSE, déjà construit pour Bookings) passé `@Public()` aussi — payload sans donnée sensible, juste jamais rendu public faute de consommateur avant. **Deuxième bug trouvé et corrigé pendant la vérification navigateur** (pas dans le scope initial) : l'auto-stamp TRANSMITTED/RECEIVED de `getPublic()` n'était pas idempotent — deux ouvertures quasi simultanées du lien chauffeur (React StrictMode en dev le déclenche de façon fiable) pouvaient toutes les deux tenter d'insérer le même `TripStep`, `@@unique([tripId, step])` faisant échouer la perdante en 500 brute. Fix d'une ligne (`skipDuplicates: true`), repéré en observant une vraie 500 dans les devtools réseau pendant la vérification, pas en lisant le code. Écart delibéré supplémentaire, identifié dans la foulée : ces deux pages activent un `retry` sur leur query (`retryPublicQuery`, `apps/web/src/features/public-tracking/`) contrairement au `retry:false` global de l'app — sans login à rediriger en cas d'échec, une page publique qui abandonne après un seul aléa réseau est *moins* robuste que le polling bête du legacy qu'elle remplace.

> **2026-08-27 — `/finance` fermé, plus aucun élément restant dans la liste de pages.** Voir `docs/handoff/2026-08-27-frontend-finance.md`. Contrairement aux autres pages, `finance.html` n'a jamais eu de logique côté legacy (`docs/LEGACY_FEATURES.md` : "Stub vide, aucune logique") — pas de spec à porter, pas d'endpoint back à exposer. Session purement structurelle : page + route + entrée nav, même gabarit que les placeholders `Driver log`/`History` d'Invoicing. Toutes les pages authentifiées et les deux pages publiques du plan sont désormais construites ; toute évolution de `/finance` au-delà de ce stub serait un nouveau scope à cadrer, pas une continuation du plan actuel.

> **2026-08-28 — Audit de parité métier legacy ↔ v2, et correction des 6 bugs qu'il a révélés.** Voir `docs/LEGACY_PARITY_AUDIT.md` (le listing complet, feature par feature / modèle par modèle) et `docs/handoff/2026-08-28-legacy-parity-audit.md`. Relecture intégrale du legacy (`server.js`, `common.js`, les 14 pages HTML) confrontée à `apps/api/src/**` + `apps/web/src/features/**`, avec pour consigne explicite de l'utilisateur : **aucun changement de logique métier ou de calcul n'est acceptable** (les écarts visuels le sont). Verdict : le cœur est fidèle (validations de course, refs `R-{client}-{YY}-{seq}` avec réutilisation des slots libérés, pipeline de statut, verrou sous-traitance, bulk Events, moteur de Gantt, catalogues identiques au nombre d'entrées près), mais **3 régressions bloquantes** et 3 bugs francs ont été trouvés et corrigés dans la foulée. La plus grave : la règle `baseVisibility` du legacy (course passée + chauffeur assigné → masquée), qui n'était appliquée **que** par `dispatcher.html`, avait été remontée en règle serveur inconditionnelle lors du passage en fenêtre de dates côté serveur (entrée journal du 2026-08-27 « Plus rien sans pagination ») — conséquence : **Invoicing ne voyait plus une seule course facturable**, son panneau Pending visant par nature les mois écoulés. Devenue un paramètre opt-in `board=true` que seule la page Bookings envoie. Deuxième : `buildTripMessageContext` lisait `pickupAt` avec les getters UTC en s'appuyant sur un commentaire faux affirmant un stockage « naïf » — tous les WhatsApp annonçaient l'heure en UTC en écrivant « (local time) » ; corrigé via Luxon dans `trip.timezone`, couvert par un test unitaire vérifié en rouge avant d'être vert. Troisième : le verrou définitif de la fiche société, **noté comme "gap accepté, documenté" dans l'entrée Settings du 2026-08-27 ci-dessus** — l'audit le requalifie en régression (le legacy rouvrait l'édition via crayon + mot de passe Owner autant de fois que voulu, `owner.html:269-280`) ; 409 retiré, l'onglet Settings affiche désormais lecture seule + crayon + Cancel. Restent ouverts et listés en §14 de l'audit, **non corrigés parce que ce sont des choix produit et pas des bugs** : éligibilité chauffeur (`driverEligibleForTrip`) et fenêtres d'indisponibilité/événement non portées (les marqueurs congé/garage sont saisissables et affichés mais ne filtrent plus rien à l'assignation), calcul de marge supprimé et sémantique de devise des tarifs inversée, auto-assignation du véhicule réservé, champ Area redevenu texte libre, brouillons d'email de sous-traitance, pré-remplissage FBO, champ texte nameboard.

> **2026-08-28 (2ᵉ passe) — les règles legacy non portées le sont, majoritairement dans le backend.** Voir `docs/LEGACY_PARITY_AUDIT.md` §14 et `docs/handoff/2026-08-28-legacy-parity-audit.md`. Recadrage explicite de l'utilisateur : le classement « choix produit » de la première passe était faux — une règle écrite dans le legacy et absente de v2 est une régression, que sa restauration coûte une ligne ou deux jours ; le coût de correction n'a pas à contaminer la nature de l'écart. Consigne associée : **un maximum de règles dans le backend**. Portées cette passe : `driverEligibleForTrip` et `isEffectivelyActive` (fenêtres d'indisponibilité + d'événement), désormais des **filtres Prisma** dans `apps/api/src/common/business/assignability.ts` exposés par `GET /drivers?availableOnly&tripClientRef&tripArea&…` et `GET /fleet-vehicles?availableOnly&compatibleWith` — en filtres et non en prédicats JS parce que ces listes sont paginées : filtrer côté front n'aurait vu que la page affichée, ce qui *masque* au lieu d'exclure. Cause racine identifiée pour ces deux règles comme pour B1 : le refactor « plus rien sans pagination » du 27/08 a déplacé les listes côté serveur, et les règles métier accrochées à ces listes n'ont pas toutes suivi — à vérifier systématiquement lors de tout futur déplacement de données vers le serveur. Également portées : auto-assignation du véhicule réservé au chauffeur (backend, un seul endroit pour create/update/drag&drop là où le legacy la dupliquait dans deux fonctions front), calcul de marge et totaux ASD (`packages/shared/src/business/pricing.js` — délibérément pas un endpoint, ce sont des indications recalculées à la frappe), filtres et désactivation du sélecteur Reg Nbr, pré-remplissage FBO (l'endpoint existait, rien ne l'appelait), champ texte `nameboard` (nouvelle colonne + migration). **Décision produit actée sur les devises** : la sémantique legacy (saisie en devise locale, stockage brut sous un champ nommé `priceEur`) **ne revient pas** — elle additionnait des euros, des francs suisses et des dollars dans un même `totalHT`, donc produisait des factures fausses hors zone euro ; mauvaise conception, pas règle métier. Le `/1.1` de la formule de marge porte sur la TVA et non sur la devise, il vaut donc inchangé en EUR. Restent ouverts et listés en §14 : champ Area contraint aux villes du pays, filtrage de la popup de rattachement à un événement, `offerEventReactivation`, brouillons d'email de sous-traitance, popups d'édition rapide (`isBeforeArrival`), `assign()` qui ne notifie pas le POC, endpoints `DELETE` sans permission.
